<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="application.test.sachin.com.gles">
    <!-- VDG : target API Level 'android-25' -->
    <uses-sdk
    android:targetSdkVersion="25"
    android:minSdkVersion="18" />
    <application
        android:icon="@mipmap/myicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/myicon_round">
        <activity android:name="application.test.sachin.com.gles.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>